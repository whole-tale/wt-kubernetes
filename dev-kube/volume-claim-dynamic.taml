apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${CLAIM_NAME}
  labels:
    app: WholeTale
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${VOLUME_SIZE}