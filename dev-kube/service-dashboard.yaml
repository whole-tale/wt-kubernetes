apiVersion: v1
kind: Service
metadata:
  name: dashboard
  labels:
    app: WholeTale
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30080
  selector:
    app: WholeTale
    component: dashboard
